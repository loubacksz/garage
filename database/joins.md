## JOINS - SQL clauses (keywords)
- Combine rows -> different tables -> based on -> related column  

### LEFT JOIN
- Returns all data from the left table
- Returns only matched data from right table
- Left table = first table from the select
- Right table = second table from the select (where you join)
- Matched data = rows that have a value for the related column

### RIGHT JOIN