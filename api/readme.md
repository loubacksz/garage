routes - a path on the server that leads to a specific resource
- they also are a part of the URL - https://example.com/myroute
                                                       |
                                                       the slash indicates the start of the route

route guy: if someone asks for this path, do this -> it might deliver a web page or do something

route path defines the endpoint

route parameters - used to capture values at specif positions in the url
- named url segments - prefixed with a colon and then the name - ex: /:yourParamName
